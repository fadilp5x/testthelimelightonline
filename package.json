{
  "name": "limelight-static-builder",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist -p 3000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
